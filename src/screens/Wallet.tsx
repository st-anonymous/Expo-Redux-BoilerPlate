import React from 'react';
import { Box, Text } from '@gluestack-ui/themed';

const Wallet = () => {
  return (
    <Box alignItems='center' justifyContent='center' h={'$full'}>
      <Text size={'5xl'}>Wallet</Text>
    </Box>
  );
};

export default Wallet;
