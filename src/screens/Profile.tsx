import React from 'react';
import { Box, Text } from '@gluestack-ui/themed';

const Profile = () => {
  return (
    <Box alignItems='center' justifyContent='center' h={'$full'}>
      <Text size={'5xl'}>Profile</Text>
    </Box>
  );
};

export default Profile;
